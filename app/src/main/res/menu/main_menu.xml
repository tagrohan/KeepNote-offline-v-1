<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/quickAdd"
        android:title="@string/add"
        android:icon="@drawable/ic_add_outline"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/quickAddImage"
        android:title="@string/add_image"
        android:icon="@drawable/ic_pic"
        app:showAsAction="ifRoom"/>
    <item android:id="@+id/quickAddLink"
        android:title="@string/add_track"
        android:icon="@drawable/ic_menu_music"
        app:showAsAction="ifRoom"/>

</menu>